const Transaction = require("../dals");

// Controller method to create transaction
module.exports = async (req, res, next) => {
  try {
    // Logic to create transaction
  } catch (error) {
    next(error);
  }
};
