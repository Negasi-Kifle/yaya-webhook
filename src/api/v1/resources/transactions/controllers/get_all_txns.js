const Transaction = require("../dals");

// Controller method to get all transactions
module.exports = async (req, res, next) => {
  try {
    // Logic to fetch all transactions
  } catch (error) {
    next(error);
  }
};
