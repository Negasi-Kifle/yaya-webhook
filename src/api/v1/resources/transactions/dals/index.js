"use strict";

// A class to hold and export all data access layers
class Transaction {
  // Create transaction
  static createTxn = require("./create_txn");
}

// Export the class
module.exports = Transaction;
